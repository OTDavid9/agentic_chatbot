import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Your other config options

  devtools: {
    enabled: false, // This disables the floating dev tools button
  },
};

export default nextConfig;